const types = {
  login: '[auth] login',
  logout: '[auth] logout',
}

export default types

/**
 * Este objeto va a contener todos los types de la aplicación.
 * Entre corchetes indicamos a que grupo de acciones pertenece el type
 */